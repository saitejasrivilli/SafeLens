# SafeLens

Production-style multimodal Trust & Safety ML system (local prototype, not TikTok-scale, not a real deployment).

Status: Phase 1 (repository foundation) complete. See `CLAUDE.md` for full spec and phase plan.

## Quickstart

```
make install
make test
```

## Docs

See `docs/` for architecture, data contract, policy taxonomy, model design, deployment, and monitoring documentation (filled in progressively as phases complete).
